var animation = bodymovin.loadAnimation({
    container: document.getElementById('ai'),
    renderer: 'svg',
    loop: false,
    autoplay: true,
    path: 'ai.json'
  })


var animation = bodymovin.loadAnimation({
    container: document.getElementById('ps'),
    renderer: 'svg',
    loop: false,
    autoplay: true,
    path: 'ps.json'
  })


var animation = bodymovin.loadAnimation({
    container: document.getElementById('ae'),
    renderer: 'svg',
    loop: false,
    autoplay: true,
    path: 'ae.json'
  })
